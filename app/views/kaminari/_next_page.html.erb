<% if current_page.last? %>
  <li class="page-item">
    <%= link_to raw(t 'views.pagination.next'), url, rel: 'next', remote: remote, class: 'page-link', disabled: true %>
  </li>
<% else %>
  <li class="page-item">
    <%= link_to raw(t 'views.pagination.next'), url, rel: 'next', remote: remote, class: 'page-link' %>
  </li>
<% end %>
